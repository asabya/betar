# Betar Documentation Site

Docusaurus 3 documentation site for the Betar project.

## Development

```bash
npm install
npm start
```

This starts a local dev server at `http://localhost:3000`.

## Build

```bash
npm run build
```

Build output is in the `build/` directory.
